# Open PS2 Loader 项目约定

## 注释

新增或改动代码时，注释一律用中文。

- 行注释、块注释、函数说明都写中文。
- 只写「为什么」和本地约定，不复述代码在做什么。
- 不要为了统一而改写上游原有的英文注释；自己新加的句子用中文。
- 路径、协议、寄存器等字面量可保留原文，说明用中文。

本仓库日常工作目录是 `C:\GitHub\Open-PS2-Loader`。Grok 会话若落在 `~/.grok/worktrees/` 下的拷贝，改动要同步回这个目录。
